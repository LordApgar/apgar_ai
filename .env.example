PORT=3000

# Each key is optional — a missing key just shows that seat as empty in the room.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Override which model sits in each seat.
OPENAI_MODEL=gpt-4o
ANTHROPIC_MODEL=claude-opus-4-8
GOOGLE_MODEL=gemini-2.0-flash
